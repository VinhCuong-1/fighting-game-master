# Copy to /etc/fighting-game.env on each game-server EC2 (once per instance or bake into AMI).
#   sudo cp fighting-game.env.example /etc/fighting-game.env
#   sudo chmod 644 /etc/fighting-game.env
#
# CI deploys fighting-game.env in the zip automatically; this file is optional on-instance override.

COGNITO_REGION=ap-southeast-1
COGNITO_USER_POOL_ID=ap-southeast-1_phYoaMUPC
MATCHES_TABLE=ActiveMatches
PORT=9000
